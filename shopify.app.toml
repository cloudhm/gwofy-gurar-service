# Shopify app configuration — sync scopes with Partner Dashboard.
# https://shopify.dev/docs/apps/tools/cli/configuration

name = "Gwofy Guard"
client_id = "YOUR_CLIENT_ID"
application_url = "https://sp-prod.gwofy.com/"
embedded = true

[access_scopes]
# Plan §1b — adjust after Partner Dashboard review (Protected Customer Data, read_all_orders, etc.)
scopes = "read_users,read_shop,read_products,write_products,read_orders,write_orders,read_markets"

[auth]
redirect_urls = [
  "https://sp-prod.gwofy.com/oauth/callback"
]

[webhooks]
# Single source of truth for topics — OAuth does not REST-register (avoids duplicate subscriptions).
api_version = "2026-04"

[[webhooks.subscriptions]]
topics = [ "app/uninstalled" ]
uri = "/webhooks/shopify"

[[webhooks.subscriptions]]
topics = [ "products/create", "products/update" ]
uri = "/webhooks/shopify"

[[webhooks.subscriptions]]
topics = [ "orders/create", "orders/updated" ]
uri = "/webhooks/shopify"

[[webhooks.subscriptions]]
topics = [ "shop/update" ]
uri = "/webhooks/shopify"

[[webhooks.subscriptions]]
topics = [ "markets/create", "markets/update" ]
uri = "/webhooks/shopify"

[[webhooks.subscriptions]]
compliance_topics = [ "customers/data_request", "customers/redact", "shop/redact" ]
uri = "/webhooks/shopify"
